# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
apps/*/dist
packages/*/dist
*.local

# Env
.env
.env.*.local
docker-compose.override.yml

# Manually-downloaded government source data for the ingestion pipeline —
# not ours to redistribute, and too large/volatile for version control.
apps/api/data/ingestion/

# Vercel function bundle, regenerated by scripts/buildFunction.mjs as part
# of the Vercel build itself (see apps/api/package.json's "vercel-build") —
# a build artifact, same treatment as dist/ above, not source.
apps/api/api/index.js

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel
.env*
